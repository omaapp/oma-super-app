import 'package:flutter/material.dart';

class AppBorders {
  AppBorders._();

  static Border primary = Border.all(
    color: const Color(0xff1565C0),
    width: 1.3,
  );

  static Border light = Border.all(
    color: const Color(0xffE8EAF6),
  );
}